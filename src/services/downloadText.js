console.log(fil - saver);
const downloadTextContent = (editor) => {
  console.log(editor);
  // Create a Blob object containing the text content.
  //   const blob = new Blob([editor], { type: "text/plain" });

  //   // Save the Blob object to the user's computer.
  //   FileSaver.saveAs(blob, "my-text-file.txt");
};
export default downloadTextContent;
